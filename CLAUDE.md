# CLAUDE.md — Claude AI Guide

This file gives Claude the context needed to work effectively in this codebase. Read it before making any changes.

---

## Commands

```bash
npm run dev          # start dev server at http://localhost:3000
npm run build        # production build (checks for type errors + build errors)
npm run lint         # ESLint via next lint
npm run typecheck    # tsc --noEmit (type check without building)
```

Validate changes with `npm run prebuild` (lint + typecheck + 88 vitest unit tests — also runs automatically before every `npm run build`). Playwright E2E lives in `tests/e2e/` (`npm run test:e2e`, needs a running dev server + auth setup).

---

## What this project is

**Farm Data Hub** — a Next.js 13 (App Router) demo that integrates with the John Deere Operations Center API. Users authenticate with Supabase, connect their John Deere account via OAuth 2.0, choose an organization, then browse their fields, boundaries, and operations (harvest, seeding) on a map-first interface.

The app is deployed to **Vercel**.

---

<!-- SCAN:AUTO:START — Generated by security-scan-prompt v7.0. Do not edit this section manually. -->

## Tech Stack

| Layer        | Tech                                                                                                                                                                                       |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Frontend     | Next.js 13.5 (App Router), React 18, TypeScript 5.2, Tailwind 3.3, shadcn/Radix UI, Mapbox GL, Recharts                                                                                    |
| Backend      | Supabase Edge Functions (Deno runtime) — John Deere OAuth + API proxy + data import; Next.js server middleware for route auth                                                              |
| Data         | Supabase Postgres (`operations_center` schema, 9 RLS-enabled tables), Supabase Storage (shapefiles bucket)                                                                                 |
| Auth         | Supabase Auth (email/password) via `@supabase/ssr` cookie sessions; John Deere OAuth 2.0 (tokens stored server-side in `john_deere_connections`)                                           |
| Hosting      | Vercel (frontend) + Supabase (edge functions, DB, storage)                                                                                                                                 |
| Testing      | Vitest (unit, `lib/__tests__` + edge `__tests__`), Playwright (e2e, `tests/e2e/`), Deno test for one edge function; 15 spec/test files; `prebuild` runs lint + typecheck + 88 vitest tests |
| Integrations | John Deere Operations Center API (`api.deere.com`, `signin.johndeere.com`), Mapbox GL JS                                                                                                   |

## Architecture

### Folder Structure

```
app/                  # Next.js App Router pages
  (app)/              # authenticated routes (map, fields, operations, applications, products, reports, settings, progress, elevation)
  auth/callback/      # OAuth callback handler
  dashboard/  login/  # entry + auth pages
components/           # UI (applications, dashboard, elevation, layout, map, overlays, reports, settings, ui)
contexts/  hooks/     # React context + hooks
lib/                  # client utilities: supabase.ts, john-deere-client.ts, area/cost/unit/elevation/report helpers
supabase/
  functions/          # Deno edge functions
    _shared/          # auth.ts, cors.ts, generic-error.ts, john-deere.ts, boundaries.ts
    john-deere-auth/  john-deere-api/  john-deere-import/  john-deere-irrigation/
  migrations/         # 22 SQL migrations (RLS-enabled tables)
types/  scripts/  tests/  __fixtures__/
middleware.ts         # server-side auth gate for (app)/* routes
```

### Key Files

- `middleware.ts` — Supabase SSR auth gate; redirects unauthenticated `(app)/*` requests to `/login` (relative-path `redirect` param, no open-redirect).
- `supabase/functions/_shared/auth.ts` — service-role client (bypasses RLS) + `getAuthenticatedUser` JWT gate; every function scopes queries by `user.id`. Only file referencing the service role.
- `supabase/functions/_shared/cors.ts` — explicit origin allowlist (no origin reflection, no credentials) for all 4 production functions.
- `supabase/functions/_shared/generic-error.ts` — generic error responder (`{error, code}` only; full error logged server-side, never returned).
- `supabase/functions/_shared/john-deere.ts` — JD token exchange/refresh (client_secret server-only via `Deno.env.get`), hardcoded JD API base + token URL constants.
- `lib/john-deere-client.ts` / `lib/supabase.ts` — browser-side clients (anon key + OAuth client ID, both public-safe).

### Data Flow

User signs in via Supabase Auth (cookie session). Middleware validates the session before serving `(app)/*` pages. The browser calls Supabase Edge Functions with the user JWT; each function authenticates the JWT, loads the user's `john_deere_connections` row via the service-role client (scoped by `user.id`), refreshes the JD access token if near expiry, then calls the John Deere API. Imported field/operation/application data is written to the `operations_center` Postgres schema and rendered on a Mapbox map and Recharts dashboards.

### External API Calls

- `POST https://signin.johndeere.com/oauth2/.../v1/token` — OAuth code exchange + refresh (server-side edge functions, on connect/refresh).
- `GET https://api.deere.com/platform/*` — organizations, fields, fieldOperations, measurement, shapefile downloads (server-side, on import/browse). URLs built from a hardcoded base + session-scoped org/field IDs or JD-returned `links[].uri` — never a client-supplied full URL.
- `GET <JD pre-signed S3 redirect>` — shapefile zip download following 307 redirect, then uploaded to Supabase Storage (irrigation function).
- Mapbox GL tiles/styles (`api.mapbox.com`, `*.tiles.mapbox.com`) — client-side map rendering.

## Environment Variables

- **Supabase (client, public-safe):** `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (RLS enforced on all tables)
- **John Deere (client):** `NEXT_PUBLIC_JOHN_DEERE_CLIENT_ID` (OAuth client ID, public by design)
- **Mapbox (client):** `NEXT_PUBLIC_MAPBOX_TOKEN`
- **Supabase Edge Functions (server-only, via `Deno.env.get`):** `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `JOHN_DEERE_CLIENT_ID`, `JOHN_DEERE_CLIENT_SECRET` — none exposed to the client bundle.

## Security Notes

### Active Flags

| Severity | Category                 | Confidence | Description                                                                                                                                                                                                                                                                                                                                                                                |
| -------- | ------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P2       | error-response-leakage   | 0.9        | `debug-field-operations` action (reachable via `john-deere-import?action=debug-field-operations`) returns a diagnostic object to the client containing raw JD upstream `response.text()` and `(err as Error).message` (`actions/debug-field-operations.ts:25,30`). Auth-gated, but leaks upstream/internal detail. Remove the stale diagnostic action or route it through `generic-error`. |
| P3       | swallowed-exception      | 1.0        | Two empty `catch {}` blocks wrapping data-write handlers in `app/(app)/settings/page.tsx:49,58` (`handleSaveOrg`, `handleDisconnect`). A failed org-select or disconnect surfaces no log, no toast, no error — the UI silently appears to succeed. Add `console.error` + a toast.                                                                                                          |
| P4       | formatting-inconsistency | 1.0        | `prettier --check .` reports code style issues in 2 files (`components/elevation/elevation-stats.tsx`, `elevation-view.tsx`). Run `npm run format`.                                                                                                                                                                                                                                        |

### Watch List (confidence < 0.8)

_None_

### Accepted Risks

| Date       | Category                 | Justification                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-10 | dependency-vulnerability | Remaining audit findings (next@13.5 advisory set, postcss, uuid-via-exceljs) are parked for the deliberate Next 16 migration sprint (TECH-DEBT.md). next CVEs are mostly DoS/cache classes behind Vercel's layered protections; exceljs is write-only (generates exports, never parses untrusted files); the only exceljs "fix" is a breaking downgrade. minimatch highs fixed 2026-06-10 via npm override (7 → 4 vulns). |
| 2026-06-10 | csp-unsafe-inline        | `'unsafe-inline'`/`'unsafe-eval'` in script-src is required by Mapbox GL + Next 13. React escaping is the primary XSS defense; all JD/LLM-derived strings render through framework escaping. Revisit with nonce-based CSP during the Next 16 migration.                                                                                                                                                                   |

### Resolved

| Date       | Category               | Note                                                                                                                                                                                                                                                                                    |
| ---------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-11 | cors-misconfig         | `debug-spray-shape` function (the only one with an inlined `Access-Control-Allow-Origin: *`) has been removed. No inline wildcard CORS remains in `supabase/functions/`; all 4 production functions use the `_shared/cors.ts` allowlist.                                                |
| 2026-06-11 | stale-debug-endpoint   | `debug-spray-shape` "TEMPORARY DIAGNOSTIC" function removed from source — attack surface eliminated.                                                                                                                                                                                    |
| 2026-06-11 | error-response-leakage | Per-action leaks in `john-deere-api` and `john-deere-irrigation` are fixed: raw upstream `errorText`/`responseBody` now flow only into `logAndRespond` server-side context, while the client receives `genericError`'s `{error, code}`. (One stale path remains — see active flag.)     |
| 2026-05-28 | cors-open              | Restricted `_shared/cors.ts` to explicit allowlist (`operations-center-api-demo.vercel.app` + localhost). Verified live: `curl -I -X OPTIONS ... -H "Origin: https://evil.com"` no longer echoes the evil origin. Deployed to all 4 functions. Commit aa76cd5 + 7942c9d (DRY refactor). |
| 2026-05-28 | error-response-leakage | Added `_shared/generic-error.ts`. All 4 functions' catch blocks now return `{error: "request_failed", code: "<FN>_<STATUS>"}` only. Server logs the full error/stack server-side. Commit 9442f2a. (Note: per-action error paths still leak — re-flagged 2026-06-05.)                    |
| 2026-05-28 | route-protection-gap   | Added `middleware.ts` using `@supabase/ssr`. Unauthenticated requests to `(app)/*` routes get 307 redirect to `/login?redirect=<path>` BEFORE any page HTML loads. Verified: `curl -i /map` returns 307. Commit e1e149c + ae0df7e (login safety comment).                               |

## Guardrails

### Universal (apply to all projects)

1. Never hardcode API keys or secrets — use `.env` + `.env.example` from first commit
2. Every API route needs CORS origin validation, input validation, and auth
3. Write RLS/Firestore rules the same day you create a table
4. Never use `innerHTML` with dynamic data — use `textContent` or framework escaping
5. API proxies must restrict destination URLs with an allowlist (SSRF prevention)
6. Auth cookies must contain signed claims (HMAC), not plain booleans
7. If a secret was ever committed to a public repo, rotate it immediately
8. No `console.log` with sensitive data in production
9. Run `npx prettier --write .` before committing — keep a `.prettierrc` in the repo root

### Project-Specific

1. John Deere OAuth tokens (access + refresh) live in `john_deere_connections` (RLS by `auth.uid()`). Edge functions read them via the **service-role** client, so they MUST keep scoping every query by `user.id` — service-role bypasses RLS.
2. `JOHN_DEERE_CLIENT_SECRET` and `SUPABASE_SERVICE_ROLE_KEY` are server-only (`Deno.env.get`). Never move them behind a `NEXT_PUBLIC_*` prefix.
3. New edge-function action handlers must return generic errors to the client (use `generic-error.ts` / a stable code), not raw `error.message` or upstream JD response bodies. The stale `debug-field-operations` action still leaks raw JD `text()` + exception messages — remove it or route through `generic-error`.
4. Any new edge function must use `_shared/cors.ts` (allowlist), not an inlined `Access-Control-Allow-Origin: *`.
5. JD API URLs are constructed server-side from the user's selected org + stored field IDs (or JD-returned `links[].uri`) — never proxy a client-supplied full URL into `fetch` (SSRF). Keep it that way.

## Deployed Surface

Production URL: https://operations-center-api-demo.vercel.app (HTTP 200). Verified headers (live, 2026-06-11):

- **Content-Security-Policy:** present (default-src 'self'; allows `'unsafe-inline'`/`'unsafe-eval'` in script-src for Mapbox/Next — see csp-unsafe-inline accepted risk)
- **Strict-Transport-Security:** `max-age=63072000; includeSubDomains; preload`
- **X-Frame-Options:** DENY | **X-Content-Type-Options:** nosniff
- **Referrer-Policy:** strict-origin-when-cross-origin | **Permissions-Policy:** camera=(), microphone=(), geolocation=(self)
- **CORS:** root page returns `Access-Control-Allow-Origin: *` (Vercel static/page surface, no credentials, not an authenticated API route). Authenticated APIs are Supabase edge functions on a separate origin using the `_shared/cors.ts` allowlist.
- **/.well-known/security.txt:** present (HTTP 200) | **robots.txt:** present (HTTP 200)
- **Server:** `Vercel` (no version leak)

## Strengths

Every Supabase Edge Function is JWT-auth-gated and scopes every query by the session-derived `user.id` (service-role never trusts client-supplied IDs), errors flow through a shared generic responder that logs server-side and returns only `{error, code}`, CORS uses an explicit origin allowlist with no reflection or credentials, all 9 `operations_center` tables have RLS enabled, and JD API URLs are built server-side from fixed constants so no user-controlled URL reaches `fetch`.

## Dev Commands

```bash
npm run dev          # next dev (http://localhost:3000)
npm run build        # next build (runs prebuild: lint + typecheck + vitest first)
npm run start        # next start (production server)
npm run lint         # next lint
npm run typecheck    # tsc --noEmit
npm run format       # prettier --write .
npm run format:check # prettier --check .
npm run test         # vitest run (unit)
npm run test:watch   # vitest (watch)
npm run test:ui      # vitest --ui
npm run test:e2e     # playwright test
npm run test:e2e:ui  # playwright test --ui
npm run test:e2e:debug # playwright test --debug
npm run test:deno    # deno test for john-deere-import edge function
```

## Metrics

- **Total lines:** ~21,350 (ts/tsx/js/css/html/json, excluding node_modules/.next/dist/build/package-lock.json/tsbuildinfo)
- **Components:** 83 | **Pages:** 15 | **API routes:** 4 Supabase edge functions (john-deere-auth, john-deere-api, john-deere-import, john-deere-irrigation)
- **Files over 500 lines:** `components/dashboard/irrigation-analysis.tsx` (660), `app/(app)/progress/page.tsx` (639), `components/reports/reports-yield-charts.tsx` (539), `components/reports/reports-view.tsx` (536), `lib/elevation-merge.ts` (510) — none over the 1500-line split threshold
- **Repo:** `https://github.com/galengrimm-code/operations-center-api-demo` (PUBLIC)
- **Production URL:** https://operations-center-api-demo.vercel.app
- **Last commit scanned:** 2026-06-11 (`9c60c20`)
- **Scan prompt version:** v7.0

<!-- SCAN:AUTO:END -->

## Rules + Doc Map

- `.claude/rules/architecture.md` (always loaded) — shared Supabase warning, key decisions, key files
- `.claude/rules/database.md` (always loaded) — `operations_center` schema (3 tables)
- `.claude/rules/conventions.md` (always loaded) — coding conventions + common task recipes
- `.claude/rules/edge-functions.md` (loads on `supabase/functions/**`) — deployment + verifyJWT rule

## Session Maintenance

When wrapping up a substantive session, propose targeted edits to CLAUDE.md or the matching rule file. Domain-specific detail belongs in the rule file. The SCAN:AUTO block is auto-managed by the `weekly-security-scan` SKILL — never hand-edit it.
